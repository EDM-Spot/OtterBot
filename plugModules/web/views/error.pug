doctype html
html(lang="en")
	head
		include includes/head.pug
		link(rel="stylesheet" href="/_/static/css/bulma.min.css")
	body
		section.hero.is-danger.is-fullheight
			div.hero-body
				div.container
					h1(class=["title"]) Error
					h2(class=["subtitle"]) #{message}