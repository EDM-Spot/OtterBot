<%- include('blocks/header', {bot, user, path}) %>
<div class="jumbotron">
  <div>
    <div class="row">
      <div class="col-md-6">
        <h1 draggable="true">EDM Spot</h1>
        <h4>
          <% if(bot.appInfo.description.length > 3) { %> <%= bot.appInfo.description %> <% } else { %>No description given. <% }%>
        </h4>
        <br>
        <h4>Now Playing:</h4>
        <iframe id="ytplayer" type="text/html" src="http://www.youtube.com/embed/<%= bot.plug.getMedia().cid %>" frameborder="0" height="300" width="500"></iframe>
      </div>
      <div class="col-md-6">
        <div class="row justify-content-center d-flex">
          <h4><i class="fas fa-crown"></i> Props LeaderBoard</h4>
        </div>
        <table class="table">
          <thead>
            <tr class="d-none">
              <th scope="col">Rank</th>
              <th scope="col">Username</th>
              <th scope="col">Props</th>
            </tr>
          </thead>
          <tbody>
            <% for (let i = 0; i < instance.length; i++) { %>
              <tr>
                <td class="text-center">
                  <%= i + 1 %>
                </td>
                <td class="text-center">
                  <%= instance[i].dataValues.username %>
                </td>
                <td class="text-center">
                  <%= instance[i].dataValues.props %>
                </td>
              </tr>
            <% } %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>
<% include ./blocks/footer %>